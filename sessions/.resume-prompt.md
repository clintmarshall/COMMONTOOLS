# Session Resume Brief

> Read this at the start of every new session. Tells you exactly where things left off.

## Where We Left Off

**Date:** 2026-07-13
**Context:** ~95% — Phase 1 complete, needs final validation

### What's Done

1. **Full Rust binary** — 12 modules, ~1.5K lines, 6 deps
   - CLI with clap (`--note`, `--json`, `--project-dir`, `--dry-run`, `--verbose`)
   - Auto-detection of fallow and vitest from project signals
   - FallowChecker: runs `fallow --format json`, parses all metrics
   - VitestChecker: runs `vitest run --coverage`, reads coverage-summary.json or Istanbul coverage-final.json
   - UnifiedMetrics struct with all fallow + coverage fields
   - qualifier.json history (append-only, versioned)
   - quality.html dashboard (embedded template, canvas charts, delta table, timeline)
   - Compat layer: updates existing fallow-chart.html and fallow-progress.md

2. **Cross-platform** — Windows npx.cmd, Unix npx, direct node_modules/.bin paths
3. **Build green** — Compiles clean, 1 harmless warning

### What Needs Doing Next (in order)

1. **Final validation** — Clean run against FileBitch, verify all 4 outputs
2. **Coverage verification** — Istanbul branch coverage against known-good data
3. **Git init** — Initialize CommonTools as git repo
4. **Security checker** — Placeholder for future (noted in design doc)
5. **`qualifier init`** — Bootstrap new projects (see `qualifier-questions.md`)
6. **Unit tests** — Fallow JSON parsing, Istanbul coverage, metrics merge

### Key Patterns

- Checker trait: `name()`, `can_run()`, `run()` → returns `CheckerOutput { version, metrics }`
- Metrics: flat `BTreeMap<String, MetricValue>` → merged into `UnifiedMetrics`
- History: `qualifier.json` with `version`, `project`, `runs[]`
- Compat: regex-based injection into existing HTML/MD files
- Cross-platform: `run_npx()` helper tries direct binary first, then npx

### Behavior Rules

- Rust binary, no async, minimal deps
- Cross-platform (Windows/WSL/Linux)
- Coexist mode: update both new (qualifier.json, quality.html) and old (fallow-chart.html, fallow-progress.md) artifacts
- Security metrics reserved in UnifiedMetrics struct (commented out)
- Distribution via alias in bashrc for now

### Open Questions (see `qualifier-questions.md`)

- `qualifier init` design (ecosystem detection, presets, tool installation)
- Checker plugin architecture (compiled-in vs dynamic)
- History retention (cap? prune?)
- CI thresholds and gates
- Schema evolution for qualifier.json
